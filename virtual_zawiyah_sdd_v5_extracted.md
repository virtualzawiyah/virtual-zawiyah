Virtual Zawiyah – Tutoring Academy System
Complete System Design Document v5.0 (Consolidated Hybrid Edition)
Public Website + Internal Platform
Target Audience: For implementation by any professional full-stack developer with zero ambiguity.
1. Project Overview
Virtual Zawiyah is a specialized online Islamic educational platform divided into:
Public Website: Landing page, course listings, trial requests, and student enrollment. Fully synced and mapped to https://virtual-zawiyah--hassaan702.replit.app/.
Internal Platform: Role-based dashboards for Admin, Academic Director, Supervisor, Teacher, Student, and Parent.
This v5.0 design implements a Custom Hybrid Architecture:
Roles (v3.0 Perfected): Strictly limited to 6 standard roles (no Super Admin, Admissions Staff, or Content Manager bloat).
Financial Model (v3.0 Perfected): Multi-currency support (PKR, USD, GBP) with dual exchange-rate logging, detailed three-tier wallets, and manual PKR conversions.
Student Status & Automation (v4.0 Perfected): Advanced student status lifecycle management (trial, active, suspended_temporary, suspended_forever, left) with security middleware blocking and status logs.
Operations (v4.0 Perfected): Automated browser-based timezone mapping, teacher work slot scheduling, workload calculations for auto-assignment, monthly bulk Excel imports, and a multi-view visual calendar.
2. Technology Stack
Layer
Technology
Frontend
Next.js 14 (App Router) + TypeScript + Tailwind CSS
Backend (BaaS)
Supabase (PostgreSQL, Supabase Auth, Supabase Storage)
Video Integration
Jitsi Meet (meet.jit.si or self-hosted)
Hosting
Vercel (Frontend + Serverless) + Supabase (Database)
Payment Gateways
Manual bank transfer (Admin-validated receipt slips)
3. Public Website Integration
The public website is fully responsive, contains zero female imagery, and routes dynamically. It integrates with the database through client-side API payloads.
3.1 Public Routes (No Auth)
/ - Hero section, platform values, highlights [Source: virtual-zawiyah--hassaan702.replit.app].
/courses - Comprehensive Islamic/Quran sciences listings.
/about - Core methodology, system values.
/pricing - Pricing packages in USD, GBP, and PKR.
/enrollment - Interactive enrollment form. Key logic: Automatically detects user’s local timezone via Intl.DateTimeFormat().resolvedOptions().timeZone and appends it to the dynamic JSON payload written to enrollment_requests.
/trial-request - Simple trial class intake form.
/contact - Support and inquiry page.
4. Complete Database Schema (SQL Script)
Run this consolidated script in the Supabase SQL Editor. It establishes the complete tables, constraints, indexes, RLS policies, and triggers for the v5.0 Hybrid architecture.
-- =====================================================================-- 1. ENUMS-- =====================================================================CREATE TYPE user_role AS ENUM ('admin', 'academic_director', 'supervisor', 'teacher', 'student', 'parent');CREATE TYPE payment_status AS ENUM ('pending', 'verified', 'rejected');CREATE TYPE withdrawal_status AS ENUM ('pending', 'transferred', 'rejected');CREATE TYPE dispute_status AS ENUM ('pending', 'resolved', 'rejected');CREATE TYPE student_status AS ENUM ('trial', 'active', 'suspended_temporary', 'suspended_forever', 'left');-- =====================================================================-- 2. CORE USER PROFILES-- =====================================================================CREATE TABLE profiles (    id UUID REFERENCES auth.users ON DELETE CASCADE PRIMARY KEY,    email TEXT UNIQUE NOT NULL,    full_name TEXT NOT NULL,    role user_role NOT NULL DEFAULT 'student',    whatsapp TEXT,    country TEXT,    city TEXT,    timezone TEXT DEFAULT 'UTC',    gender TEXT CHECK (gender IN ('male', 'female')),    avatar_url TEXT,    student_status student_status DEFAULT 'active',    status_change_reason TEXT,    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL,    updated_at TIMESTAMPTZ DEFAULT NOW() NOT NULL);-- Auto-update trigger for profilesCREATE OR REPLACE FUNCTION update_updated_at()RETURNS TRIGGER AS $$BEGIN     NEW.updated_at = NOW();     RETURN NEW; END;$$ LANGUAGE plpgsql;CREATE TRIGGER trg_profiles_updated_at    BEFORE UPDATE ON profiles    FOR EACH ROW EXECUTE FUNCTION update_updated_at();-- =====================================================================-- 3. ENROLLMENT & TRIAL REQUESTS-- =====================================================================CREATE TABLE enrollment_requests (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    student_name TEXT NOT NULL,    student_age INT,    parent_name TEXT NOT NULL,    parent_email TEXT NOT NULL,    parent_whatsapp TEXT,    course_interest TEXT,    preferred_teacher_id UUID REFERENCES profiles(id) ON DELETE SET NULL,    message TEXT,    timezone TEXT DEFAULT 'UTC',    status TEXT DEFAULT 'pending' CHECK (status IN ('pending', 'reviewed', 'enrolled', 'rejected')),    assigned_teacher_id UUID REFERENCES profiles(id) ON DELETE SET NULL,    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL);CREATE INDEX idx_enrollment_requests_status ON enrollment_requests(status);CREATE TABLE trial_requests (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    student_id UUID REFERENCES profiles(id) ON DELETE CASCADE,    teacher_id UUID REFERENCES profiles(id) ON DELETE SET NULL,    student_name TEXT,    parent_email TEXT,    requested_date DATE NOT NULL,    timezone TEXT DEFAULT 'UTC',    status TEXT CHECK (status IN ('pending', 'scheduled', 'completed', 'cancelled')) DEFAULT 'pending' NOT NULL,    class_link TEXT,    feedback TEXT,    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL);CREATE INDEX idx_trial_requests_status ON trial_requests(status);-- =====================================================================-- 4. ACADEMIC AND SCHEDULING TABLES (v3.0 Structured)-- =====================================================================CREATE TABLE teacher_student_assignments (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    teacher_id UUID REFERENCES profiles(id) ON DELETE RESTRICT NOT NULL,    student_id UUID REFERENCES profiles(id) ON DELETE CASCADE NOT NULL,    assigned_date DATE DEFAULT CURRENT_DATE NOT NULL,    end_date DATE,    is_active BOOLEAN DEFAULT TRUE NOT NULL,    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL);CREATE INDEX idx_assignments_teacher_active ON teacher_student_assignments(teacher_id, is_active);CREATE INDEX idx_assignments_student_active ON teacher_student_assignments(student_id, is_active);CREATE TABLE class_schedules (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    assignment_id UUID REFERENCES teacher_student_assignments(id) ON DELETE CASCADE NOT NULL,    day_of_week INT NOT NULL CHECK (day_of_week BETWEEN 0 AND 6),    start_time TIME NOT NULL, -- Stored locally/UTC    duration_minutes INT DEFAULT 30 NOT NULL,    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL);CREATE INDEX idx_schedules_assignment ON class_schedules(assignment_id);CREATE TABLE academic_calendar (    date DATE PRIMARY KEY,    is_teaching_day BOOLEAN DEFAULT TRUE NOT NULL,    description TEXT);CREATE INDEX idx_academic_calendar_date ON academic_calendar(date);-- =====================================================================-- 5. ATTENDANCE & LESSON LOGGING-- =====================================================================CREATE TABLE attendance_logs (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    teacher_id UUID REFERENCES profiles(id) ON DELETE CASCADE NOT NULL,    student_id UUID REFERENCES profiles(id) ON DELETE CASCADE NOT NULL,    class_date DATE DEFAULT CURRENT_DATE NOT NULL,    status TEXT CHECK (status IN ('present', 'absent', 'leave')) NOT NULL, -- v3.0 Simple attendance    locked BOOLEAN DEFAULT FALSE NOT NULL,    notes TEXT,    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL,    UNIQUE(student_id, class_date));CREATE INDEX idx_attendance_teacher_date ON attendance_logs(teacher_id, class_date);CREATE INDEX idx_attendance_student_date ON attendance_logs(student_id, class_date);CREATE TABLE attendance_unlock_log (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    attendance_id UUID REFERENCES attendance_logs(id) ON DELETE CASCADE NOT NULL,    unlocked_by UUID REFERENCES profiles(id) ON DELETE RESTRICT NOT NULL,    reason TEXT NOT NULL,    unlocked_at TIMESTAMPTZ DEFAULT NOW() NOT NULL);CREATE TABLE lesson_logs (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    teacher_id UUID REFERENCES profiles(id) ON DELETE CASCADE NOT NULL,    student_id UUID REFERENCES profiles(id) ON DELETE CASCADE NOT NULL,    class_date DATE DEFAULT CURRENT_DATE NOT NULL,    topic_covered TEXT NOT NULL,    next_plan TEXT NOT NULL,    performance TEXT CHECK (performance IN ('excellent', 'good', 'average', 'needs_improvement')) NOT NULL,    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL);CREATE INDEX idx_lesson_logs_teacher_date ON lesson_logs(teacher_id, class_date);CREATE INDEX idx_lesson_logs_student_date ON lesson_logs(student_id, class_date);CREATE TABLE lesson_private_notes (    lesson_id UUID REFERENCES lesson_logs(id) ON DELETE CASCADE PRIMARY KEY,    notes TEXT NOT NULL,    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL);-- =====================================================================-- 6. v4.0 OPERATIONAL TABLES (Workslots, History, Imports)-- =====================================================================CREATE TABLE teacher_work_slots (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    teacher_id UUID REFERENCES profiles(id) ON DELETE CASCADE NOT NULL,    day_of_week INT NOT NULL CHECK (day_of_week BETWEEN 0 AND 6),    slot_start TIME NOT NULL,    slot_end TIME NOT NULL,    break_start TIME,    break_end TIME,    is_active BOOLEAN DEFAULT TRUE,    created_at TIMESTAMPTZ DEFAULT NOW());CREATE INDEX idx_teacher_work_slots ON teacher_work_slots(teacher_id, day_of_week);CREATE TABLE student_status_history (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    student_id UUID REFERENCES profiles(id) ON DELETE CASCADE NOT NULL,    previous_status student_status NOT NULL,    new_status student_status NOT NULL,    changed_by UUID REFERENCES profiles(id) ON DELETE RESTRICT NOT NULL,    reason TEXT,    changed_at TIMESTAMPTZ DEFAULT NOW());CREATE TABLE student_status_import_logs (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    file_name TEXT NOT NULL,    file_url TEXT NOT NULL,    uploaded_by UUID REFERENCES profiles(id) ON DELETE RESTRICT NOT NULL,    month_year DATE NOT NULL,    records_imported INT DEFAULT 0,    status TEXT CHECK (status IN ('pending', 'completed', 'failed')) DEFAULT 'pending',    error_message TEXT,    created_at TIMESTAMPTZ DEFAULT NOW());-- =====================================================================-- 7. v3.0 FINANCIAL TABLES (Multi-Currency & Three-Tier Wallets)-- =====================================================================CREATE TABLE teacher_wallet (    teacher_id UUID REFERENCES profiles(id) ON DELETE CASCADE PRIMARY KEY,    total_earned NUMERIC(12, 2) DEFAULT 0.00 NOT NULL CHECK (total_earned >= 0),    available_balance NUMERIC(12, 2) DEFAULT 0.00 NOT NULL CHECK (available_balance >= 0),    total_withdrawn NUMERIC(12, 2) DEFAULT 0.00 NOT NULL CHECK (total_withdrawn >= 0),    currency TEXT DEFAULT 'PKR' NOT NULL);CREATE TABLE parent_payments (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    parent_id UUID REFERENCES profiles(id) ON DELETE RESTRICT NOT NULL,    total_amount NUMERIC(12, 2) NOT NULL,    currency TEXT NOT NULL CHECK (currency IN ('USD', 'GBP', 'PKR')),    receipt_url TEXT NOT NULL,    reference_number TEXT,    status payment_status DEFAULT 'pending' NOT NULL,    verified_at TIMESTAMPTZ,    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL);CREATE INDEX idx_parent_payments_status ON parent_payments(status);CREATE INDEX idx_parent_payments_parent ON parent_payments(parent_id);CREATE TABLE fee_payments (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    parent_payment_id UUID REFERENCES parent_payments(id) ON DELETE CASCADE,    student_id UUID REFERENCES profiles(id) ON DELETE RESTRICT NOT NULL,    teacher_id UUID REFERENCES profiles(id) ON DELETE RESTRICT NOT NULL,    month_year TEXT NOT NULL, -- Formatted as 'YYYY-MM'    original_currency TEXT NOT NULL CHECK (original_currency IN ('USD', 'GBP', 'PKR')),    original_amount NUMERIC(12, 2) NOT NULL,    pkr_amount NUMERIC(12, 2), -- Populated manually by admin upon verification    commission_amount NUMERIC(12, 2), -- Calculated as 10%    teacher_amount NUMERIC(12, 2), -- Calculated as 90%    status payment_status DEFAULT 'pending' NOT NULL,    receipt_url TEXT,    reference_number TEXT,    verified_at TIMESTAMPTZ,    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL,    CONSTRAINT unique_student_month UNIQUE (student_id, month_year));CREATE INDEX idx_fee_payments_status ON fee_payments(status);CREATE INDEX idx_fee_payments_teacher ON fee_payments(teacher_id);CREATE INDEX idx_fee_payments_student_month ON fee_payments(student_id, month_year);CREATE TABLE withdrawal_requests (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    teacher_id UUID REFERENCES profiles(id) ON DELETE RESTRICT NOT NULL,    amount NUMERIC(12, 2) NOT NULL CHECK (amount > 0),    bank_name TEXT NOT NULL,    account_iban TEXT NOT NULL,    status withdrawal_status DEFAULT 'pending' NOT NULL,    transfer_ref TEXT,    processed_at TIMESTAMPTZ,    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL);CREATE INDEX idx_withdrawal_requests_teacher_status ON withdrawal_requests(teacher_id, status);CREATE TABLE wallet_transactions (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    teacher_id UUID REFERENCES profiles(id) ON DELETE RESTRICT NOT NULL,    type TEXT CHECK (type IN ('fee_credit', 'withdrawal_debit', 'adjustment')) NOT NULL,    amount NUMERIC(12, 2) NOT NULL,    balance_after NUMERIC(12, 2) NOT NULL,    description TEXT NOT NULL,    reference_id UUID,    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL);CREATE INDEX idx_wallet_transactions_teacher_created ON wallet_transactions(teacher_id, created_at DESC);CREATE TABLE exchange_rate_log (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    date DATE DEFAULT CURRENT_DATE NOT NULL,    usd_to_pkr NUMERIC(8, 2) NOT NULL,    gbp_to_pkr NUMERIC(8, 2) NOT NULL,    entered_by_admin_id UUID REFERENCES profiles(id) ON DELETE RESTRICT NOT NULL,    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL);-- =====================================================================-- 8. DISPUTES (v3.0 Linked to Fee Payments)-- =====================================================================CREATE TABLE disputes (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    teacher_id UUID REFERENCES profiles(id) ON DELETE RESTRICT NOT NULL,    fee_payment_id UUID REFERENCES fee_payments(id) ON DELETE CASCADE NOT NULL,    reason TEXT NOT NULL,    status dispute_status DEFAULT 'pending' NOT NULL,    admin_response TEXT,    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL);-- =====================================================================-- 9. NOTIFICATIONS-- =====================================================================CREATE TABLE notifications (    id UUID DEFAULT gen_random_uuid() PRIMARY KEY,    user_id UUID REFERENCES profiles(id) ON DELETE CASCADE NOT NULL,    role user_role NOT NULL,    title TEXT NOT NULL,    message TEXT NOT NULL,    is_read BOOLEAN DEFAULT FALSE NOT NULL,    link TEXT,    created_at TIMESTAMPTZ DEFAULT NOW() NOT NULL);CREATE INDEX idx_notifications_user_read ON notifications(user_id, is_read, created_at DESC);
5. RLS Policies (Row Level Security)
-- ProfilesALTER TABLE profiles ENABLE ROW LEVEL SECURITY;CREATE POLICY "Users read own profile" ON profiles FOR SELECT USING (id = auth.uid());CREATE POLICY "Users update own profile" ON profiles FOR UPDATE USING (id = auth.uid());CREATE POLICY "Admin read all profiles" ON profiles FOR SELECT USING ((SELECT role FROM profiles WHERE id = auth.uid()) = 'admin');CREATE POLICY "Admin update all profiles" ON profiles FOR UPDATE USING ((SELECT role FROM profiles WHERE id = auth.uid()) = 'admin');-- EnrollmentALTER TABLE enrollment_requests ENABLE ROW LEVEL SECURITY;CREATE POLICY "Anyone insert enrollment" ON enrollment_requests FOR INSERT WITH CHECK (true);CREATE POLICY "Admin read enrollment" ON enrollment_requests FOR SELECT USING ((SELECT role FROM profiles WHERE id = auth.uid()) = 'admin');CREATE POLICY "Admin update enrollment" ON enrollment_requests FOR UPDATE USING ((SELECT role FROM profiles WHERE id = auth.uid()) = 'admin');-- Teacher Work SlotsALTER TABLE teacher_work_slots ENABLE ROW LEVEL SECURITY;CREATE POLICY "Teacher own slots" ON teacher_work_slots FOR ALL USING (teacher_id = auth.uid());CREATE POLICY "Admin read slots" ON teacher_work_slots FOR SELECT USING ((SELECT role FROM profiles WHERE id = auth.uid()) = 'admin');-- Student Status History & Import LogsALTER TABLE student_status_history ENABLE ROW LEVEL SECURITY;CREATE POLICY "Admin read status history" ON student_status_history FOR SELECT USING ((SELECT role FROM profiles WHERE id = auth.uid()) = 'admin');ALTER TABLE student_status_import_logs ENABLE ROW LEVEL SECURITY;CREATE POLICY "Admin read import logs" ON student_status_import_logs FOR ALL USING ((SELECT role FROM profiles WHERE id = auth.uid()) = 'admin');-- Attendance & LessonsALTER TABLE attendance_logs ENABLE ROW LEVEL SECURITY;CREATE POLICY "Teacher owns attendance" ON attendance_logs USING (teacher_id = auth.uid());CREATE POLICY "Student reads own attendance" ON attendance_logs FOR SELECT USING (student_id = auth.uid());CREATE POLICY "Admin management attendance" ON attendance_logs FOR ALL USING ((SELECT role FROM profiles WHERE id = auth.uid()) = 'admin');-- Financial PermissionsALTER TABLE teacher_wallet ENABLE ROW LEVEL SECURITY;CREATE POLICY "Teacher read own wallet" ON teacher_wallet FOR SELECT USING (teacher_id = auth.uid());CREATE POLICY "Admin manage wallets" ON teacher_wallet FOR ALL USING ((SELECT role FROM profiles WHERE id = auth.uid()) = 'admin');ALTER TABLE parent_payments ENABLE ROW LEVEL SECURITY;CREATE POLICY "Parent own payments" ON parent_payments USING (parent_id = auth.uid());CREATE POLICY "Admin management parent payments" ON parent_payments FOR ALL USING ((SELECT role FROM profiles WHERE id = auth.uid()) = 'admin');
6. Database Triggers & Workload Views
6.1 Teacher Workload View (v4.0 Logic)
Calculates workload metrics based on actual occupied weekly calendar schedules instead of raw student count.
CREATE VIEW teacher_weekly_workload ASSELECT     tsa.teacher_id,    p.full_name AS teacher_name,    COUNT(DISTINCT cs.id) AS weekly_slots_occupiedFROM teacher_student_assignments tsaJOIN class_schedules cs ON cs.assignment_id = tsa.idJOIN profiles p ON p.id = tsa.teacher_idWHERE tsa.is_active = trueGROUP BY tsa.teacher_id, p.full_name;
6.2 Student Status Change Log Trigger (v4.0 Logic)
CREATE OR REPLACE FUNCTION log_student_status_change()RETURNS TRIGGER AS $$BEGIN  IF OLD.student_status IS DISTINCT FROM NEW.student_status THEN    INSERT INTO student_status_history (student_id, previous_status, new_status, changed_by, reason)    VALUES (NEW.id, OLD.student_status, NEW.student_status, auth.uid(), NEW.status_change_reason);  END IF;  RETURN NEW;END;$$ LANGUAGE plpgsql SECURITY DEFINER;CREATE TRIGGER trg_student_status_changeAFTER UPDATE OF student_status ON profilesFOR EACH ROW EXECUTE FUNCTION log_student_status_change();
6.3 Verification of Parent Payment (v3.0 Logic)
CREATE OR REPLACE FUNCTION process_verified_parent_payment()RETURNS TRIGGER AS $$BEGIN    IF NEW.status = 'verified' AND OLD.status = 'pending' THEN        UPDATE fee_payments        SET status = 'verified', verified_at = NOW()        WHERE parent_payment_id = NEW.id;    END IF;    RETURN NEW;END;$$ LANGUAGE plpgsql SECURITY DEFINER;CREATE TRIGGER trg_parent_payment_verified    AFTER UPDATE ON parent_payments    FOR EACH ROW EXECUTE FUNCTION process_verified_parent_payment();
6.4 Fee Payment Verification Trigger (v3.0 Multi-Currency Splitting)
Handles the manual calculations entered by Admin. Splits fee (90/10) in PKR, registers the transaction, and logs a notification.
CREATE OR REPLACE FUNCTION process_verified_fee()RETURNS TRIGGER AS $$DECLARE wallet_row RECORD;BEGIN    IF NEW.status = 'verified' AND OLD.status = 'pending' THEN        -- Compute 10% commission fee & 90% teacher allocation inside manual PKR entries        NEW.commission_amount := NEW.pkr_amount * 0.10;        NEW.teacher_amount    := NEW.pkr_amount * 0.90;        NEW.verified_at       := NOW();        -- Initialize wallet if empty        INSERT INTO teacher_wallet (teacher_id, total_earned, available_balance, total_withdrawn, currency)        VALUES (NEW.teacher_id, 0, 0, 0, 'PKR') ON CONFLICT DO NOTHING;        -- Update Wallet balances        UPDATE teacher_wallet        SET total_earned      = total_earned      + NEW.teacher_amount,            available_balance = available_balance + NEW.teacher_amount        WHERE teacher_id = NEW.teacher_id        RETURNING available_balance INTO wallet_row;        -- Record credit transactions        INSERT INTO wallet_transactions            (teacher_id, type, amount, balance_after, description, reference_id)        VALUES            (NEW.teacher_id, 'fee_credit', NEW.teacher_amount, wallet_row.available_balance,             'Fee credit for ' || NEW.month_year || ' Student ID: ' || NEW.student_id, NEW.id);        -- Send System Notifications        INSERT INTO notifications (user_id, role, title, message)        VALUES (NEW.teacher_id, 'teacher', 'Fee Payment Credited',                'PKR ' || NEW.teacher_amount || ' credited to your available balance.');    END IF;    RETURN NEW;END;$$ LANGUAGE plpgsql SECURITY DEFINER;CREATE TRIGGER trg_fee_verified    BEFORE UPDATE ON fee_payments    FOR EACH ROW EXECUTE FUNCTION process_verified_fee();
6.5 Withdrawal Lifecycle Triggers (v3.0 Logic)
-- Reserve balance on insertCREATE OR REPLACE FUNCTION process_withdrawal_request_insert()RETURNS TRIGGER AS $$DECLARE current_balance NUMERIC(12,2);BEGIN    SELECT available_balance FROM teacher_wallet    WHERE teacher_id = NEW.teacher_id FOR UPDATE INTO current_balance;    IF current_balance < NEW.amount THEN        RAISE EXCEPTION 'Insufficient balance to request withdrawal';    END IF;    UPDATE teacher_wallet    SET available_balance = available_balance - NEW.amount    WHERE teacher_id = NEW.teacher_id;    RETURN NEW;END;$$ LANGUAGE plpgsql SECURITY DEFINER;CREATE TRIGGER trg_withdrawal_requested    BEFORE INSERT ON withdrawal_requests    FOR EACH ROW EXECUTE FUNCTION process_withdrawal_request_insert();-- Finalize transaction upon payment transfer or rejectCREATE OR REPLACE FUNCTION process_withdrawal_request_update()RETURNS TRIGGER AS $$DECLARE wallet_row RECORD;BEGIN    IF OLD.status = 'pending' AND NEW.status = 'transferred' THEN        UPDATE teacher_wallet        SET total_withdrawn = total_withdrawn + NEW.amount        WHERE teacher_id = NEW.teacher_id        RETURNING available_balance INTO wallet_row;        INSERT INTO wallet_transactions            (teacher_id, type, amount, balance_after, description, reference_id)        VALUES            (NEW.teacher_id, 'withdrawal_debit', NEW.amount, wallet_row.available_balance,             'Completed withdrawal reference: ' || COALESCE(NEW.transfer_ref, 'Bank Transfer'), NEW.id);    ELSIF OLD.status = 'pending' AND NEW.status = 'rejected' THEN        UPDATE teacher_wallet        SET available_balance = available_balance + NEW.amount        WHERE teacher_id = NEW.teacher_id        RETURNING available_balance INTO wallet_row;        INSERT INTO wallet_transactions            (teacher_id, type, amount, balance_after, description, reference_id)        VALUES            (NEW.teacher_id, 'adjustment', NEW.amount, wallet_row.available_balance,             'Refunded rejected withdrawal', NEW.id);    END IF;    RETURN NEW;END;$$ LANGUAGE plpgsql SECURITY DEFINER;CREATE TRIGGER trg_withdrawal_processed    AFTER UPDATE ON withdrawal_requests    FOR EACH ROW EXECUTE FUNCTION process_withdrawal_request_update();
7. Frontend Page Structure (Next.js 14 App Router)
Structure accommodates all features across strictly 6 roles. Administrative submodules (Excel export, student state archives, backup) are localized inside the /admin root.
app/├── (public)/│   ├── page.js                    # Connected to Replit design layouts│   ├── courses/page.js            # Active courses list│   ├── about/page.js│   ├── pricing/page.js│   ├── enrollment/page.js         # Browser timezone auto-detected│   ├── trial-request/page.js│   └── contact/page.js├── login/page.js├── middleware.js                  # Protects directories & flags blocks├── teacher/│   ├── dashboard/page.js          # Class logs & summaries│   ├── attendance/page.js         # Mark attendances (Locked within 24h)│   ├── work-slots/page.js         # Manage availability work slots│   └── wallet/page.js             # Withdrawals requests & transactions list├── student/│   ├── dashboard/page.js│   ├── schedule/page.js           # Multi-timezone adjusted visual calendar│   └── fee-payment/page.js        # Receipt upload gateway├── parent/│   ├── dashboard/page.js│   └── payments/page.js           # View active balances & split allocations├── admin/│   ├── dashboard/page.js          # Admin central metrics│   ├── fee-verification/page.js   # PKR manual allocation, verified triggers│   ├── withdrawals/page.js        # Transfer settlement triggers│   ├── assignments/page.js        # Student/teacher assignment, auto-assign button│   ├── enrollment-requests/page.js# Converts submissions into parent profiles│   ├── calendar/page.js           # Visual calendar (Day, Week, User views)│   ├── excel-import/page.js       # Bulk XLSX state parser│   └── backups/page.js            # Automated system snapshot console└── lib/    └── supabaseClient.js
8. Critical Code Blocks
8.1 Advanced Middleware Protection (v4.0 Logic)
Inspects role credentials and active student_status. Suspended or left users are dynamically signed out and blocked from dashboard domains.
// middleware.jsimport { createMiddlewareClient } from '@supabase/auth-helpers-nextjs'import { NextResponse } from 'next/server'export async function middleware(req) {  const res = NextResponse.next()  const supabase = createMiddlewareClient({ req, res })  const { data: { user } } = await supabase.auth.getUser()  const publicPaths = ['/', '/courses', '/about', '/pricing', '/enrollment', '/trial-request', '/contact', '/login']  const isPublicPath = publicPaths.includes(req.nextUrl.pathname)  if (!user && !isPublicPath) {    return NextResponse.redirect(new URL('/login', req.url))  }  if (user) {    const { data: profile } = await supabase      .from('profiles')      .select('role, student_status')      .eq('id', user.id)      .single()    // Immediately block suspended or deleted users    if (profile && ['suspended_temporary', 'suspended_forever', 'left'].includes(profile.student_status)) {      await supabase.auth.signOut()      return NextResponse.redirect(new URL('/login?blocked=true', req.url))    }    const role = profile?.role    const path = req.nextUrl.pathname    // Ensure strict routing limits matching exactly the 6 core roles    if (path.startsWith('/teacher') && role !== 'teacher')       return NextResponse.redirect(new URL('/login', req.url))    if (path.startsWith('/student') && role !== 'student')       return NextResponse.redirect(new URL('/login', req.url))    if (path.startsWith('/parent') && role !== 'parent')       return NextResponse.redirect(new URL('/login', req.url))    if (path.startsWith('/admin') && role !== 'admin')       return NextResponse.redirect(new URL('/login', req.url))  }  return res}
8.2 Bulk Student Status Excel Import (v4.0 Logic)
// app/api/excel-import/route.jsimport { createClient } from '@supabase/supabase-js'import * as XLSX from 'xlsx'export async function POST(req) {  const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY)  const formData = await req.formData()  const file = formData.get('file')  const uploadedBy = formData.get('uploaded_by')  const buffer = await file.arrayBuffer()  const workbook = XLSX.read(buffer, { type: 'array' })  const sheet = workbook.Sheets[workbook.SheetNames[0]]  const rows = XLSX.utils.sheet_to_json(sheet)  let successCount = 0  for (const row of rows) {    const { student_email, student_status, reason } = row    const { data: profile } = await supabase.from('profiles').select('id').eq('email', student_email).single()    if (profile) {      await supabase.from('profiles')        .update({           student_status: student_status.toLowerCase(),           status_change_reason: reason         })        .eq('id', profile.id)      successCount++    }  }  await supabase.from('student_status_import_logs').insert({    file_name: file.name,    file_url: 'internal_bucket_imports_location',    uploaded_by: uploadedBy,    month_year: new Date().toISOString().slice(0, 10),    records_imported: successCount,    status: 'completed'  })  return Response.json({ status: 'success', imported: successCount })}
8.3 Auto-Assign Teacher API
Calculates assignments via the database teacher_weekly_workload view, evaluating teacher schedules to return the lowest workload teacher.
// app/api/auto-assign/route.jsimport { createClient } from '@supabase/supabase-js'export async function POST(req) {  const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY)    const { data: teachers } = await supabase.from('profiles').select('id').eq('role', 'teacher')  const teacherIds = teachers.map(t => t.id)  const { data: workload, error } = await supabase    .from('teacher_weekly_workload')    .select('*')    .in('teacher_id', teacherIds)    .order('weekly_slots_occupied', { ascending: true })  if (workload && workload.length > 0) {    return Response.json({ status: 'success', suggested_teacher_id: workload[0].teacher_id })  }    // Fallback: If no schedules recorded yet, suggest first teacher  return Response.json({ status: 'success', suggested_teacher_id: teacherIds[0] })}
9. Developer Operational Guidelines
Clone & Setup: Standard Next.js installation. Set environment variables correctly inside .env.local.
Initial Database Execution: Run the consolidated PostgreSQL schema from Section 4 in the Supabase SQL editor.
Turn Off Email Signup Confirmations: Turn off inside Supabase UI Dashboard (Authentication -> Providers -> Email -> toggling off "Confirm Email").
Storage Creation: Generate private storage buckets receipts (unauthenticated files blocked) and imports (Excel logs).
Set Local Crons / Reminders: Configure automated verification cycles using Cron services like Vercel Crons or Supabase pg_cron extensions.
-- Cron: Automated Reminders on days 1st, 5th, and 10th of the monthSELECT cron.schedule('monthly-student-import-reminder', '0 9 1,5,10 * *', $$  INSERT INTO notifications (user_id, role, title, message)  SELECT id, 'admin', 'Monthly Student Import Required',         'The student status Excel archive for ' || to_char(NOW(), 'Month YYYY') || ' needs processing.'  FROM profiles WHERE role = 'admin'  AND NOT EXISTS (    SELECT 1 FROM student_status_import_logs     WHERE date_trunc('month', month_year) = date_trunc('month', NOW())  )$$);
-- Cron: Automated Attendance Lock (24 hours lock period)SELECT cron.schedule('daily-attendance-lock', '0 0 * * *', $$  UPDATE attendance_logs  SET locked = TRUE  WHERE class_date < (CURRENT_DATE - INTERVAL '1 day')    AND locked = FALSE;$$);
-- Cron: Daily Backup System (Targeting admin backups dashboard log metadata)SELECT cron.schedule('daily-backup-status', '0 2 * * *', $$  INSERT INTO notifications (user_id, role, title, message)  SELECT id, 'admin', 'Daily Database Integrity Check', 'Automated standard system snapshots completed.'  FROM profiles WHERE role = 'admin';$$);
10. Core Implementation Checklist
[ ] Public Site Sync: Ensure core forms route to /enrollment and /trial-request matching virtual-zawiyah--hassaan702.replit.app.
[ ] Dynamic Timezone Capturing: Timezones are scraped client-side during registration and preserved on user signup profiles.
[ ] Status Transition Rules: Validating student blocks; updating student profiles to suspended successfully blocks active sessions.
[ ] XLSX Status Import Pipeline: Test upload pipeline with valid/invalid structures.
[ ] Attendance Manual Locks: Teachers should not be able to update entries marked beyond the 24-hour interval.
[ ] Multi-Currency Splits & Wallets: Check that parent multi-currency values are successfully computed, verified, and settled with 90% allocation applied to teacher wallets.
[ ] Visual Multi-view Calendars: Daily, weekly, teacher, and student calendars operate dynamically in UTC conversions.
